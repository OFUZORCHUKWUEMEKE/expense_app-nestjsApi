import { CustomInterceptorInterceptor } from './custom-interceptor.interceptor';

describe('CustomInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new CustomInterceptorInterceptor()).toBeDefined();
  });
});
